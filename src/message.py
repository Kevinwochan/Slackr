def send(token, channel_id, message):
    return {
        'message_id': 1,
    }

def remove(token, message_id):
    return {
    }

def edit(token, message_id, message):
    return {
    }